# Pinned dependencies for the processor Lambda deployment package.
# boto3 is omitted — it is pre-installed in the Lambda runtime.
# Update these versions to match requirements-lock.txt when upgrading deps.
anthropic==0.86.0
pillow==12.1.1
psycopg2-binary==2.9.11
