AWSTemplateFormatVersion: '2010-09-09'
Description: Photo Tagger — Processor Lambda

Parameters:
  DeploymentBucket:
    Type: String
    Description: S3 bucket containing the Lambda deployment zip

  DeploymentKey:
    Type: String
    Default: processor.zip
    Description: S3 key of the Lambda deployment zip

  NeonDatabaseUrl:
    Type: String
    NoEcho: true
    Description: Neon PostgreSQL connection string

  AnthropicApiKey:
    Type: String
    NoEcho: true
    Description: Anthropic API key

  PhotosBucket:
    Type: String
    Description: S3 bucket the Lambda reads source photos from

  InboxBucket:
    Type: String
    Description: S3 bucket for unprocessed inbox photos

Resources:
  ProcessorRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: S3ReadPhotos
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: s3:GetObject
                Resource:
                  - !Sub 'arn:aws:s3:::${PhotosBucket}/*'
                  - !Sub 'arn:aws:s3:::${InboxBucket}/*'
              - Effect: Allow
                Action: s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::${PhotosBucket}'
                  - !Sub 'arn:aws:s3:::${InboxBucket}'

  ProcessorFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub '${AWS::StackName}-processor'
      Runtime: python3.12
      Handler: handler.lambda_handler
      Role: !GetAtt ProcessorRole.Arn
      Timeout: 120
      MemorySize: 512
      ReservedConcurrentExecutions: 10
      Code:
        S3Bucket: !Ref DeploymentBucket
        S3Key: !Ref DeploymentKey
      Environment:
        Variables:
          NEON_DATABASE_URL: !Ref NeonDatabaseUrl
          ANTHROPIC_API_KEY: !Ref AnthropicApiKey

  ProcessorEventBridgeRule:
    Type: AWS::Events::Rule
    Properties:
      EventPattern:
        source:
          - aws.s3
        detail-type:
          - Object Created
        detail:
          bucket:
            name:
              - !Ref PhotosBucket
              - !Ref InboxBucket
      Targets:
        - Id: ProcessorLambdaTarget
          Arn: !GetAtt ProcessorFunction.Arn

  ProcessorEventBridgePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt ProcessorFunction.Arn
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt ProcessorEventBridgeRule.Arn

Outputs:
  ProcessorFunctionName:
    Description: Lambda function name (use as PROCESSOR_LAMBDA_NAME in .env)
    Value: !Ref ProcessorFunction

  ProcessorFunctionArn:
    Description: Lambda function ARN (used by photos-bucket stack for S3 notifications)
    Value: !GetAtt ProcessorFunction.Arn
